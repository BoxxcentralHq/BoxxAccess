# BoxxAccess Device Probe

This project will be the first executable built in BoxxAccess. It validates a physical terminal connection safely before the gateway service or staff UI is introduced.

It is intentionally read-only in its initial version. See `../../docs/device-setup.md` for the procedure and safety constraints.
